spring:
  main:
    banner-mode: OFF
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: root
    jpa:
      hibernate:
        ddl-auto: none
      show-sql: false



dgs:
  graphql:
    schema-locations:
    - classpath*:schema/**/*.graphql*
    - classpath*:schema/**/*.gql